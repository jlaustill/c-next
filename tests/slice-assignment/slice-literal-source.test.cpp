/**
 * Generated by C-Next Transpiler from: slice-literal-source.test.cnx
 * A safer C for embedded systems
 */

#include <stdint.h>

// test-execution
// Issue #1085 review (Finding D): a bare integer literal is a valid slice source,
// contextually typed to the slice byte-width (ADR-052). buf[0,4] <- 0x12345678
// writes the 4 bytes little-endian. Previously rejected with a self-contradictory
// "source must be an integer value, but ... is assigned a 'int'" message.
int main(void) {
    uint8_t buf[8] = {};
    /* MISRA C:2012 Rule 21.15: slice copy unrolled to per-element writes (memcpy would pass incompatible pointer types: uint8_t* vs uint32_t*). */
    const uint32_t _tmp0 = (uint32_t)(0x12345678);
    buf[0] = (uint8_t)(_tmp0);
    buf[1] = (uint8_t)(_tmp0 >> 8U);
    buf[2] = (uint8_t)(_tmp0 >> 16U);
    buf[3] = (uint8_t)(_tmp0 >> 24U);
    if (buf[0U] != 0x78) return 1;
    if (buf[1U] != 0x56) return 2;
    if (buf[2U] != 0x34) return 3;
    if (buf[3U] != 0x12) return 4;
    return 0;
}
