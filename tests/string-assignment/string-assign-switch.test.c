/**
 * Generated by C-Next Transpiler from: string-assign-switch.test.cnx
 * A safer C for embedded systems
 */

#include "string-assign-switch.test.h"

#include <stdint.h>
#include <string.h>

// test-execution
// Issue #139: Test string assignment inside switch cases
char level[33] = "";

void setLevel(uint32_t code) {
    switch (code) {
        case 0: {
            (void) strncpy(level, "Low", 32); level[32] = '\0';
            break;
        }
        case 1: {
            (void) strncpy(level, "Medium", 32); level[32] = '\0';
            break;
        }
        case 2: {
            (void) strncpy(level, "High", 32); level[32] = '\0';
            break;
        }
        default: {
            (void) strncpy(level, "Unknown", 32); level[32] = '\0';
            break;
        }
    }
}

int main(void) {
    setLevel(0U);
    if (strlen(level) != 3) return 1U;
    setLevel(1U);
    if (strlen(level) != 6) return 2U;
    setLevel(2U);
    if (strlen(level) != 4) return 3U;
    setLevel(99U);
    if (strlen(level) != 7) return 4U;
    return 0U;
}
