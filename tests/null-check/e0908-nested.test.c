/**
 * Generated by C-Next Transpiler from: e0908-nested.test.cnx
 * A safer C for embedded systems
 */

// E0908: Nested if blocks with NULL checks
#include <stdio.h>

#include <string.h>

char buffer[257] = "";

void testNested(void) {
    FILE* c_file = fopen("test.txt", "r");
    if (c_file != NULL) {
        char* c_line = fgets(buffer, 257, c_file);
        if (c_line != NULL) {
            printf("%s", c_line);
        }
        fclose(c_file);
    }
}
