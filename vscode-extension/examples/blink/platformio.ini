[env:teensymm]
platform = teensy
board = teensymm
framework = arduino

; Pre-build script to transpile c-next files
extra_scripts = 
    pre:build_cnext.py

; Generated C files are copied to src/ by the pre-build script

; Monitor settings
monitor_speed = 115200