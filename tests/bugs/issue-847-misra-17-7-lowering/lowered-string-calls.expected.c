/**
 * Generated by C-Next Transpiler from: lowered-string-calls.test.cnx
 * A safer C for embedded systems
 */

#include "lowered-string-calls.test.h"

#include <string.h>

// test-c-only
// #847 / ADR-070 Case 1: calls the TRANSPILER emits for string lowering must be
// explicitly (void)-cast to satisfy MISRA C:2012 Rule 17.7. The author never
// wrote these calls, so the cast is codegen -- not a language change (Case 2).
char greeting[17] = "";

char names[2][17] = {0};

void assignSimple(void) {
    (void) strncpy(greeting, "hello", 16); greeting[16] = '\0';
}

void assignArrayElement(void) {
    (void) strncpy(names[0], "world", 16);
}
