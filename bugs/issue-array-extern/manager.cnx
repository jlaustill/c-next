// Test: array with constant from external file
#include "config.cnx"

struct TReading {
    i16 value;
    bool valid;
}

bool enabled[DEVICE_COUNT] <- [false, false, false, false];
TReading readings[DEVICE_COUNT][4];

scope Manager {
    public void test() {
        global.enabled[0] <- true;
    }
}
