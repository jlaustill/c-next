/**
 * Generated by C-Next Transpiler from: cross-file-direct.test.cnx
 * A safer C for embedded systems
 */

#include "cross-file-direct.test.h"

// test-execution
// test-adr: 057, 016
// ADR-057 resolution in a fixture that INCLUDES another file (depth 1).
//
// No ADR-057 fixture had an `#include` at all, so the `imported direct` and
// `imported transitive` columns had no fixture -- the rule was tested only
// where every declaration sat in one file. A check that holds in one context
// routinely fails in another (#1219), and the include boundary is exactly the
// axis this matrix exists to watch.
#include "cross-file-provider.h"

#include <stdint.h>

// ADR-044: Overflow helper functions
#include <limits.h>

/* ADR-044 / Issue #94: the second parameter is the WIDER type, not the value type.
   Narrowing it first would let an out-of-range operand truncate INTO range and defeat
   the check: cnx_clamp_add_u8(0, 256) must saturate to 255, but (uint8_t)256 is 0, so a
   uint8_t parameter would return 0 -- the opposite of saturation. */

static inline uint32_t cnx_clamp_add_u32(uint32_t a, uint64_t b) {
    if (b > (uint64_t)(UINT32_MAX - a)) return UINT32_MAX;
    return (uint32_t)(a + (uint32_t)b);
}

/* Scope: Gauge */
static uint32_t Gauge__total = 0U;
static uint32_t Gauge__step = 2U;
static uint32_t Gauge__primed = 3;

uint32_t Gauge__bump(void) {
    return Gauge__step;
}

void Gauge__accumulate(void) {
    Gauge__total = cnx_clamp_add_u32(Gauge__total, Gauge__bump());
}

uint32_t Gauge__readTotal(void) {
    return Gauge__total;
}

// A top-level function with a shadowing local: ADR-057 with no enclosing scope,
// in a fixture that reaches another file. `shadow-outside-any-scope` covers this
// same-file; the include boundary is a separate matrix column.
uint32_t spare = 40U;

uint32_t detached(void) {
    uint32_t detached__spare = 6U;
    return detached__spare;
}

int main(void) {
    Sensor__refresh();
    uint32_t sensorValue = Sensor__value();
    if (sensorValue != 10) return 1;
    Gauge__accumulate();
    Gauge__accumulate();
    uint32_t gaugeTotal = Gauge__readTotal();
    if (gaugeTotal != 4) return 2;
    uint32_t detachedValue = detached();
    if (detachedValue != 6) return 3;
    if (spare != 40) return 4;
    return 0;
}
