/**
 * Generated by C-Next Transpiler from: global-type-param.test.cnx
 * A safer C for embedded systems
 */

#include "global-type-param.test.h"

#include <stdint.h>

// test-execution
// Tests: global.EnumType as function parameter type inside scope
// Issue #478: global.EnumType not recognized as type reference inside scope
// Define enum in same file to avoid cross-file header conflicts
/* Scope: Processor */

int32_t Processor_process(EPriority priority) {
    if ((int32_t)priority == 0) return 10;
    if ((int32_t)priority == 1) return 20;
    return 30;
}

int main(void) {
    int32_t result = Processor_process(EPriority_LOW);
    if (result != 10) return 1;
    result = Processor_process(EPriority_MEDIUM);
    if (result != 20) return 2;
    result = Processor_process(EPriority_HIGH);
    if (result != 30) return 3;
    return 0;
}
