// The intermediate hop. It re-exports nothing deliberately: `deep-use` reaches
// `DeepReading` and `deepValue` THROUGH this file, which is the relationship
// the `imported transitive` row is about.
#include "deep-lib.cnx"

u32 viaDeep() {
    return deepValue();
}
