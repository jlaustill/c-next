/**
 * Generated by C-Next Transpiler from: macro-through-cnx-include.test.cnx
 * A safer C for embedded systems
 */

#include "macro-through-cnx-include.test.hpp"

// NEGATIVE CONTROL for the #1399 review — this file must stay SILENT.
//
// Both names come from a C header two hops away: this file includes only a
// .cnx, and that file includes the header. The first precondition walked only
// THIS file's #include lines, saw nothing foreign, kept E0427 enabled and
// rejected SHARED_LIMIT_MACRO — code that main transpiles and gcc compiles.
// Include visibility is transitive, so the precondition must be too.
//
// The typedef is the contrast that isolates the cause: shared_point_t was
// never flagged, because a parsed header typedef does reach the run-wide
// table. Only the macro has nothing to fall back on.
#include <macro-bridge.hpp>

#include <stdint.h>

shared_point_t sharedOrigin = {};

int main(void) {
    uint32_t limit = SHARED_LIMIT_MACRO;
    if (limit != 42) {
        return 1;
    }
    return 0;
}
