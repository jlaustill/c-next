/**
 * Generated by C-Next Transpiler from: slice-literal-negative-source.test.cnx
 * A safer C for embedded systems
 */

#include <stdint.h>

// test-execution
// Issue #1085 review: a negative integer literal is a valid in-range slice source,
// contextually typed to the slice byte-width (ADR-052) and serialized as
// two's-complement little-endian bytes. buf[0,4] <- -1 writes 0xFF in all 4 bytes.
int main(void) {
    uint8_t buf[8] = {};
    /* MISRA C:2012 Rule 21.15: slice copy unrolled to per-element writes (memcpy would pass incompatible pointer types: uint8_t* vs uint32_t*). */
    const uint32_t _tmp0 = (uint32_t)(-1);
    buf[0] = (uint8_t)(_tmp0);
    buf[1] = (uint8_t)(_tmp0 >> 8U);
    buf[2] = (uint8_t)(_tmp0 >> 16U);
    buf[3] = (uint8_t)(_tmp0 >> 24U);
    if (buf[0U] != 0xFF) return 1;
    if (buf[1U] != 0xFF) return 2;
    if (buf[2U] != 0xFF) return 3;
    if (buf[3U] != 0xFF) return 4;
    return 0;
}
