/**
 * Generated by C-Next Transpiler from: array-member-read.test.cnx
 * A safer C for embedded systems
 */

#include "array-member-read.test.h"

#include <stdint.h>

// ADR-044: Overflow helper functions
#include <limits.h>

/* ADR-044 / Issue #94: the second parameter is the WIDER type, not the value type.
   Narrowing it first would let an out-of-range operand truncate INTO range and defeat
   the check: cnx_clamp_add_u8(0, 256) must saturate to 255, but (uint8_t)256 is 0, so a
   uint8_t parameter would return 0 -- the opposite of saturation. */

static inline uint32_t cnx_clamp_add_u32(uint32_t a, uint64_t b) {
    if (b > (uint64_t)(UINT32_MAX - a)) return UINT32_MAX;
    return (uint32_t)(a + (uint32_t)b);
}

// test-execution
// Regression test: reading array members within structs in loops
// Verifies postfix chain ordering: cfg.items[i].value
int main(void) {
    Container cfg = {0};
    Item item0 = { .value = 100U, .flags = 0U };
    Item item1 = { .value = 200U, .flags = 0U };
    Item item2 = { .value = 300U, .flags = 0U };
    cfg.items[0] = item0;
    cfg.items[1] = item1;
    cfg.items[2] = item2;
    uint32_t sum = 0U;
    for (uint32_t i = 0; i < 3; i += 1) {
        sum = cnx_clamp_add_u32(sum, cfg.items[i].value);
    }
    if (sum != 600) {
        return 1;
    }
    return 0;
}
