// test-error
// Signed shift operations are rejected at compile time (MISRA C:2012 Rule 10.1)
// Left-shifting signed values is undefined behavior in C
// Right-shifting signed values is implementation-defined in C
void main() {
    i32 a <- 1000;
    i32 result <- a << 4; // E0805: signed shift not allowed
}
