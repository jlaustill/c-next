/**
 * Generated by C-Next Transpiler
 * A safer C for embedded systems
 */

#include <stdint.h>
#include <limits.h>

// test-execution
// Tests: f64 to u32 cast should clamp values to 0-4294967295 range
int main(void) {
    double over = 5000000000.0;
    double negative = -100.0;
    double in_range = 3000000000.0;
    uint32_t result = ((over) > ((double)UINT32_MAX) ? UINT32_MAX : (over) < 0.0 ? 0 : (uint32_t)(over));
    if (result != 4294967295) return 1;
    result = ((negative) > ((double)UINT32_MAX) ? UINT32_MAX : (negative) < 0.0 ? 0 : (uint32_t)(negative));
    if (result != 0) return 2;
    result = ((in_range) > ((double)UINT32_MAX) ? UINT32_MAX : (in_range) < 0.0 ? 0 : (uint32_t)(in_range));
    if (result != 3000000000) return 3;
    return 0;
}
