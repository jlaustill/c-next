/**
 * Generated by C-Next Transpiler from: compound-assign-use-before-init.test.cnx
 * A safer C for embedded systems
 */

#include <stdint.h>

// ADR-044: Overflow helper functions
#include <limits.h>

static inline uint32_t cnx_clamp_add_u32(uint32_t a, uint64_t b) {
    if (b > (uint64_t)(UINT32_MAX - a)) return UINT32_MAX;
    uint32_t result;
    if (__builtin_add_overflow(a, (uint32_t)b, &result)) return UINT32_MAX;
    return result;
}

// test-error
// Test: Compound assignment should check use-before-init on LHS
// Issue #1012: Compound assignment bypasses E0381 check
void demo(void) {
    uint32_t sum = 0;
    sum = cnx_clamp_add_u32(sum, 5U);
}
