// ADR-047: Valid NULL check pattern for fgets
#include <stdio.h>

string<64> buffer;

void readInput() {
    if (fgets(buffer, buffer.size, stdin) != NULL) {
        printf("Got: %s", buffer);
    }
}
