// FAILS: same global const, but the object is declared inside a scope.
// Error: Constructor argument 'SENSOR_CHIP_SELECT_PIN' must be const.
//        C++ constructors in C-Next only accept const variables.
// It IS const -- declared identically to works.cnx above.
#include <FakeSensor.h>

const u8 SENSOR_CHIP_SELECT_PIN <- 10;

scope Fails {
    FakeSensor sensor(SENSOR_CHIP_SELECT_PIN);

    bool start() { return sensor.begin(); }
}
