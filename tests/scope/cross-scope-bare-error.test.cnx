// test-error
// Test: Cross-scope access to private member should error
// ADR-016: Private scope members cannot be accessed from other scopes

scope Counter {
    i32 value <- 100;  // Private by default
}

scope Manager {
    void test() {
        // ERROR: value is private in Counter scope
        Counter.value <- 50;
    }
}

void main() {
}
