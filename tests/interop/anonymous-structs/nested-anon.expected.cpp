/**
 * Generated by C-Next Transpiler
 * A safer C for embedded systems
 */

// test-cpp-only
// test-execution
// Test: Nested anonymous structs (anonymous inside anonymous)
#include "anon_types.h"

#include <stdint.h>

int main(void) {
    ThemeConfig theme = (ThemeConfig){ .colors = { .foreground = { .r = 255, .g = 128, .b = 64 }, .background = { .r = 0, .g = 32, .b = 16 } } };
    if (theme.colors.foreground.r != 255) return 1;
    if (theme.colors.foreground.g != 128) return 2;
    if (theme.colors.foreground.b != 64) return 3;
    if (theme.colors.background.r != 0) return 4;
    if (theme.colors.background.g != 32) return 5;
    if (theme.colors.background.b != 16) return 6;
    ThemeConfig theme2 = (ThemeConfig){ .colors = { .foreground = { .r = 200, .g = 200, .b = 200 } } };
    if (theme2.colors.foreground.r != 200) return 7;
    if (theme2.colors.foreground.g != 200) return 8;
    if (theme2.colors.foreground.b != 200) return 9;
    return 0;
}
