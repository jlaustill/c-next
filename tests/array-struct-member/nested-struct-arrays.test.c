/**
 * Generated by C-Next Transpiler from: nested-struct-arrays.test.cnx
 * A safer C for embedded systems
 */

#include "nested-struct-arrays.test.h"

#include <stdint.h>

// test-execution
// Tests: Nested structs containing array members
// Coverage: Array access through nested struct paths
int main(void) {
    Path path = {0};
    path.count = 3U;
    path.points[0].x = 0;
    path.points[0].y = 0;
    path.points[1].x = 10;
    path.points[1].y = 20;
    path.points[2].x = 30;
    path.points[2].y = 40;
    if (path.count != 3) return 1U;
    if (path.points[0U].x != 0) return 2U;
    if (path.points[0U].y != 0) return 3U;
    if (path.points[1U].x != 10) return 4U;
    if (path.points[1U].y != 20) return 5U;
    if (path.points[2U].x != 30) return 6U;
    if (path.points[2U].y != 40) return 7U;
    Route route = {0};
    route.totalSegments = 2U;
    route.segments[0].count = 2U;
    route.segments[0].points[0].x = 100;
    route.segments[0].points[0].y = 100;
    route.segments[0].points[1].x = 150;
    route.segments[0].points[1].y = 150;
    route.segments[1].count = 2U;
    route.segments[1].points[0].x = 200;
    route.segments[1].points[0].y = 200;
    route.segments[1].points[1].x = 250;
    route.segments[1].points[1].y = 250;
    if (route.totalSegments != 2) return 8U;
    if (route.segments[0U].count != 2) return 9U;
    if (route.segments[0U].points[0U].x != 100) return 10U;
    if (route.segments[0U].points[1U].y != 150) return 11U;
    if (route.segments[1U].points[0U].x != 200) return 12U;
    if (route.segments[1U].points[1U].y != 250) return 13U;
    uint32_t segIdx = 1U;
    uint32_t ptIdx = 0U;
    if (route.segments[segIdx].points[ptIdx].x != 200) return 14U;
    route.segments[0].points[0].x += 50;
    if (route.segments[0U].points[0U].x != 150) return 15U;
    return 0U;
}
