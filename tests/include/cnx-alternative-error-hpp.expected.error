5:0 error[E0504]: Found #include "cnx-alt-helper-cpp.hpp" but 'cnx-alt-helper-cpp.cnx' exists at the same location.
       Use #include "cnx-alt-helper-cpp.cnx" instead to use the C-Next version.
       help: The generated header describes the interface; the C-Next source is what the transpiler can check calls against (ADR-010).
