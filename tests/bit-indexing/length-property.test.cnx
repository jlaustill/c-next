// ADR-007: The .length property
// Tests: .length returns bit width for integers and element count for arrays

void main() {
    u8 byte <- 0;
    u16 word <- 0;
    u32 dword <- 0;
    u8[64] buffer;
    buffer[0] <- 0;  // Initialize to satisfy use-before-init check

    u32 byteBits <- byte.bit_length;    // 8
    u32 wordBits <- word.bit_length;    // 16
    u32 dwordBits <- dword.bit_length;  // 32
    u32 bufLen <- buffer.element_count;    // 64
}
