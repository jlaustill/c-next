/**
 * Generated by C-Next Transpiler from: adr-001-relay.cnx
 * A safer C for embedded systems
 */

#include "adr-001-relay.h"

// Intermediate link for the transitive ADR-001 fixture: a fixture including
// THIS file reaches adr-001-consts.cnx at depth 2, which the matrix calls
// "imported transitive".
//
// It declares a constant of its own rather than only re-exporting. A .cnx whose
// entire content is an `#include` emits no header while its consumers still
// `#include` one, so the generated C does not compile (#1234) -- unrelated to
// ADR-001, and every relay in this corpus declares something for that reason.
#include "adr-001-consts.h"

#include <stdint.h>

const uint8_t SIX = 6U;
