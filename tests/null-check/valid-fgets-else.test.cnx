// ADR-047: Valid NULL check with else branch
#include <stdio.h>

string<128> line;

void processLine() {
    i8* result <- fgets(line, line.size, stdin);
    if (result != NULL) {
        printf("Read: %s", line);
    } else {
        printf("No more input\n");
    }
}
