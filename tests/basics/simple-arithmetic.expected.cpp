/**
 * Generated by C-Next Transpiler from: simple-arithmetic.test.cnx
 * A safer C for embedded systems
 */

#include <stdint.h>

// test-execution
// Tests: Basic arithmetic operators
// Demonstrates: +, -, *, /, % with different types
int main(void) {
    uint32_t a = 10U;
    uint32_t b = 20U;
    uint32_t sum = a + b;
    if (sum != 30) return 1;
    uint32_t diff = b - a;
    if (diff != 10) return 2;
    uint32_t product = a * b;
    if (product != 200) return 3;
    uint32_t quotient = b / a;
    if (quotient != 2) return 4;
    uint32_t remainder = 17U % 5U;
    if (remainder != 2) return 5;
    uint32_t result = (a + b) * 2U;
    if (result != 60) return 6;
    result = a + b * 2U;
    if (result != 50) return 7;
    int32_t x = -10;
    int32_t y = 3;
    int32_t signedSum = x + y;
    if (signedSum != -7) return 8;
    int32_t signedProduct = x * y;
    if (signedProduct != -30) return 9;
    float f1 = 10.5;
    float f2 = 3.0;
    float fsum = f1 + f2;
    if (fsum < 13.4 || fsum > 13.6) return 10;
    float fdiv = f1 / f2;
    if (fdiv < 3.4 || fdiv > 3.6) return 11;
    return 0;
}
