/**
 * Generated by C-Next Transpiler
 * A safer C for embedded systems
 */

#include <stdint.h>
#include <stdbool.h>

/* test-execution */
// Test i16 bitwise operations: AND, OR, XOR, NOT
// Coverage: Section 5.1-5.4 for i16 type
uint32_t main(void) {
    int16_t a = 0x7F00;
    int16_t b = 0x5555;
    int16_t and_result = a & b;
    int16_t neg_a = -256;
    int16_t neg_b = -1;
    int16_t neg_and = neg_a & neg_b;
    int16_t or_result = a | b;
    int16_t xor_result = a ^ b;
    int16_t c = 32767;
    int16_t not_pos = ~c;
    int16_t d = -32768;
    int16_t not_neg = ~d;
    int16_t minus_one = -1;
    int16_t not_minus_one = ~minus_one;
    if (and_result == 21760 && or_result == 32597 && xor_result == 10837) {
        if (not_pos == -32768 && not_neg == 32767 && not_minus_one == 0) {
            if (neg_and == -256) {
                return 0;
            }
        }
    }
    return 1;
}
