/**
 * Generated by C-Next Transpiler from: esp-style.test.cnx
 * A safer C for embedded systems
 */

#include "esp-style.test.h"

// test-transpile-only
// Issue #957: ESP-IDF style handle with no struct definition
//
// When typedef is `typedef struct X *Y` and struct X is NEVER defined,
// the type is ALREADY a pointer. No extra * should be added.
#include "esp_style.h"

#include <stdint.h>

/* Scope: SPI */
static spi_device_handle_t SPI__device = {0};

void SPI__init(void) {
    (void)spi_device_init(&SPI__device);
}

void SPI__write(uint8_t data) {
    spi_device_write(SPI__device, data);
}

int main(void) {
    SPI__init();
    SPI__write(42U);
    return 0;
}
