// ADR-027: Do-while with equality condition

const u8 END_MARKER <- 0xFF;

void readUntilEnd() {
    u8 byte <- 0;
    do {
        byte <- 0x42;  // Simulated read
    } while (byte != END_MARKER);
}
