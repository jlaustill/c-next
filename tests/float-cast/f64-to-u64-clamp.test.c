/**
 * Generated by C-Next Transpiler
 * A safer C for embedded systems
 */

#include <stdint.h>
#include <limits.h>

// test-execution
// Tests: f64 to u64 cast should clamp negative values to 0
// Note: f64 can't represent values > UINT64_MAX precisely, so we test negative clamping
int main(void) {
    double negative = -100.0;
    double in_range = 1000000000000.0;
    uint64_t result = ((negative) > ((double)UINT64_MAX) ? (uint64_t)UINT64_MAX : (negative) < 0.0 ? 0 : (uint64_t)(negative));
    if (result != 0) return 1;
    result = ((in_range) > ((double)UINT64_MAX) ? (uint64_t)UINT64_MAX : (in_range) < 0.0 ? 0 : (uint64_t)(in_range));
    if (result != 1000000000000) return 2;
    return 0;
}
