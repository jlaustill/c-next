// Block 1 of scope Span (#1334). Four files declare one scope; each block must be
// recorded as its own declaration site.
scope Span {
    public struct Point {
        u32 x;
        u32 y;
    }
}
