#ifndef TICKS_H
#define TICKS_H

#include <stdint.h>

/* A scalar typedef from a C header: the transpiler knows the name resolves,
   but not that what it resolves to is scalar rather than an aggregate. */
typedef uint16_t Ticks;

#endif
