/**
 * Generated by C-Next Transpiler from: shadow-global-from-scope-method.test.cnx
 * A safer C for embedded systems
 */

#include "shadow-global-from-scope-method.test.hpp"

#include <stdint.h>

// ADR-044: Overflow helper functions
#include <limits.h>

/* ADR-044 / Issue #94: the second parameter is the WIDER type, not the value type.
   Narrowing it first would let an out-of-range operand truncate INTO range and defeat
   the check: cnx_clamp_add_u8(0, 256) must saturate to 255, but (uint8_t)256 is 0, so a
   uint8_t parameter would return 0 -- the opposite of saturation. */

static inline uint32_t cnx_clamp_add_u32(uint32_t a, uint64_t b) {
    if (b > (uint64_t)(UINT32_MAX - a)) return UINT32_MAX;
    return (uint32_t)(a + (uint32_t)b);
}

// test-execution
// test-adr: 057
// ADR-057: bare names resolve local -> scope -> global, AND `global.` must see
// PAST a shadow. C has no `::`, so a local that shadows a file-scope name is
// emitted under a distinct C identifier; were it not, `global.count` would
// silently bind to the local and compile clean (#1289).
//
// Every value below is distinct so a wrong binding cannot pass by coincidence.
uint32_t count = 1000U;

uint32_t buf[2] = {7U, 8U};

uint32_t tick = 99U;

/* Scope: Counter */
static uint32_t Counter__count = 100U;

uint32_t Counter__plain(void) {
    uint32_t fresh = 5U;
    return fresh;
}

uint32_t Counter__allThreeLevels(void) {
    uint32_t Counter__allThreeLevels__count = 10U;
    return cnx_clamp_add_u32(cnx_clamp_add_u32(Counter__allThreeLevels__count, Counter__count), count);
}

uint32_t Counter__arrayShadow(void) {
    uint32_t Counter__arrayShadow__buf[2] = {1U, 2U};
    return Counter__arrayShadow__buf[0U] + buf[0U];
}

uint32_t Counter__loopShadow(void) {
    uint32_t sum = 0U;
    for (uint32_t Counter__loopShadow__tick = 0; Counter__loopShadow__tick < 2; Counter__loopShadow__tick += 1) {
        sum = cnx_clamp_add_u32(sum, tick);
    }
    return sum;
}

int main(void) {
    uint32_t a = Counter__allThreeLevels();
    if (a != 1110) return 1;
    uint32_t b = Counter__arrayShadow();
    if (b != 8) return 2;
    uint32_t c = Counter__loopShadow();
    if (c != 198) return 3;
    uint32_t d = Counter__plain();
    if (d != 5) return 4;
    return 0;
}
