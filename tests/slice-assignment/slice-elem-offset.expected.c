/**
 * Generated by C-Next Transpiler from: slice-elem-offset.test.cnx
 * A safer C for embedded systems
 */

#include <stdint.h>

// test-execution
// Issue #1081 review: for arrays wider than a byte, the slice OFFSET is an
// element index (matching the old &arr[offset] base) while the LENGTH is a byte
// count. A non-zero offset must land on element boundaries, leaving earlier
// elements untouched.
int main(void) {
    uint16_t arr[8] = {0};
    uint32_t v = 0x11223344U;
    /* MISRA C:2012 Rule 21.15: slice copy unrolled to per-element writes (memcpy would pass incompatible pointer types: uint16_t* vs uint32_t*). */
    arr[2] = (uint16_t)(v);
    arr[3] = (uint16_t)(v >> 16U);
    if (arr[0U] != 0x0000) return 1;
    if (arr[1U] != 0x0000) return 2;
    if (arr[2U] != 0x3344) return 3;
    if (arr[3U] != 0x1122) return 4;
    return 0;
}
