// Tests: Float function parameter modulo should be detected as error
// Parameters with float types using % should fail
void compute(f32 value) {
    f32 result <- value % 2;
}

void main() {
    compute(10.5);
}
