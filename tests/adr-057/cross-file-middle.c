/**
 * Generated by C-Next Transpiler from: cross-file-middle.cnx
 * A safer C for embedded systems
 */

#include "cross-file-middle.h"

// Middle link of the ADR-057 transitive chain. Not a test itself.
//
// Includes the provider, so a fixture including THIS file sits two hops from
// `cross-file-provider.cnx` and lands in the `imported transitive` column.
#include "cross-file-provider.h"

#include <stdint.h>

// ADR-044: Overflow helper functions
#include <limits.h>

/* ADR-044 / Issue #94: the second parameter is the WIDER type, not the value type.
   Narrowing it first would let an out-of-range operand truncate INTO range and defeat
   the check: cnx_clamp_add_u8(0, 256) must saturate to 255, but (uint8_t)256 is 0, so a
   uint8_t parameter would return 0 -- the opposite of saturation. */

static inline uint32_t cnx_clamp_add_u32(uint32_t a, uint64_t b) {
    if (b > (uint64_t)(UINT32_MAX - a)) return UINT32_MAX;
    return (uint32_t)(a + (uint32_t)b);
}

/* Scope: Filter */
static uint32_t Filter__window = 4U;

uint32_t Filter__half(void) {
    return Filter__window / 2;
}

uint32_t Filter__smooth(uint32_t sample) {
    uint32_t h = Filter__half();
    return cnx_clamp_add_u32(sample, h);
}
