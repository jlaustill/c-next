/**
 * Generated by C-Next Transpiler from: rvalue-arg-cpp-loop.test.cnx
 * A safer C for embedded systems
 */

// test-execution
// Tests: Issue #250 edge case - rvalue expressions in control flow conditions
// Verifies temp var placement before control flow statements (not inside body)
#include "rvalue-arg-cpp.h"

#include <stdint.h>

uint32_t checkValue(uint8_t x) {
    return x;
}

int main(void) {
    uint8_t a = 5U;
    uint8_t b = 3U;
    uint32_t result = 0U;
    uint32_t check1 = checkValue(a + b);
    if (check1 > 5) {
        result = 1U;
    }
    if (result != 1) return 1;
    uint8_t iterations = 0U;
    uint8_t limit = 2U;
    uint32_t check2 = checkValue(limit + 1U);
    while (check2 > iterations) {
        iterations = iterations + 1U;
        if (iterations > 10) return 2;
    }
    if (iterations != 3) return 3;
    uint32_t sum = 0U;
    uint8_t maxVal = 3U;
    uint32_t check3 = checkValue(maxVal + 1U);
    for (uint8_t i = 0; i < check3; i = i + 1) {
        sum = sum + 1U;
    }
    if (sum != 4) return 4;
    return 0;
}
