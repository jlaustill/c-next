/**
 * Generated by C-Next Transpiler from: switch-relay.cnx
 * A safer C for embedded systems
 */

#include "switch-relay.h"

// Helper (not a test): puts switch-declarations.cnx two hops from a consumer,
// covering the `imported transitive` column. It declares something of its own
// so a header is written for it (#1234).
#include "switch-declarations.h"

#include <stdint.h>

const uint32_t SWITCH_RELAY_MARKER = 2U;
