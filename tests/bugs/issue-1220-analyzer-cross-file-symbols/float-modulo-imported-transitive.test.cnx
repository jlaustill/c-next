// test-error
// E0804: modulo on a floating-point operand -- matrix cell: imported transitive (2 hops)

#include "relay.cnx"

f32 globalRemainder <- floatValue % 2;

f32 topLevelFunction() {
    f32 remainder <- floatValue % 2;
    return remainder;
}

scope Widget {
    f32 memberRemainder <- floatValue % 2;

    f32 methodValue() {
        f32 remainder <- floatValue % 2;
        return remainder;
    }
}
