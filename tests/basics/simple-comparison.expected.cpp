/**
 * Generated by C-Next Transpiler from: simple-comparison.test.cnx
 * A safer C for embedded systems
 */

#include <stdint.h>
#include <stdbool.h>

// test-execution
// Tests: Comparison and logical operators
// Demonstrates: =, !=, <, >, <=, >=, &&, ||, !
int main(void) {
    uint32_t a = 10U;
    uint32_t b = 20U;
    uint32_t c = 10U;
    bool equal = (a == c);
    if (equal != true) return 1U;
    equal = (a == b);
    if (equal != false) return 2U;
    bool notEqual = (a != b);
    if (notEqual != true) return 3U;
    bool less = (a < b);
    if (less != true) return 4U;
    less = (b < a);
    if (less != false) return 5U;
    bool greater = (b > a);
    if (greater != true) return 6U;
    bool lessEq = (a <= c);
    if (lessEq != true) return 7U;
    lessEq = (a <= b);
    if (lessEq != true) return 8U;
    bool greaterEq = (a >= c);
    if (greaterEq != true) return 9U;
    bool both = (a < b && b > 0);
    if (both != true) return 10U;
    both = (a < b && b < 0);
    if (both != false) return 11U;
    bool either = (a > b || b > 0);
    if (either != true) return 12U;
    either = (a > b || b < 0);
    if (either != false) return 13U;
    bool flag = true;
    bool notFlag = !flag;
    if (notFlag != false) return 14U;
    notFlag = !false;
    if (notFlag != true) return 15U;
    bool complex = ((a == c) && (b > a)) || false;
    if (complex != true) return 16U;
    return 0U;
}
