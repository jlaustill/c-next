#ifndef DISCARD_CPP_LIB_HPP
#define DISCARD_CPP_LIB_HPP
/* Non-void C++ function. .hpp symbols land in a different index from .h ones,
   so this half needs its own fixture -- it was silently exempt. */
int cppLibGetValue(void);
#endif
