// ADR-047: Valid NULL check pattern for fgets
#include <stdio.h>

string<64> buffer;

void readInput() {
    i8* result <- fgets(buffer, buffer.size, stdin);
    if (result != NULL) {
        printf("Got: %s", buffer);
    }
}
