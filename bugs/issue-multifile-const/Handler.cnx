#include "Config.cnx"
#include "Storage.cnx"

scope Handler {
    // Calls Storage.loadDefaults which modifies cfg
    public u8 reset(Config cfg) {
        global.Storage.loadDefaults(cfg);
        return 0;
    }
}
