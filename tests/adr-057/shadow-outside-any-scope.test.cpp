/**
 * Generated by C-Next Transpiler from: shadow-outside-any-scope.test.cnx
 * A safer C for embedded systems
 */

#include "shadow-outside-any-scope.test.hpp"

#include <stdint.h>

// ADR-044: Overflow helper functions
#include <limits.h>

/* ADR-044 / Issue #94: the second parameter is the WIDER type, not the value type.
   Narrowing it first would let an out-of-range operand truncate INTO range and defeat
   the check: cnx_clamp_add_u8(0, 256) must saturate to 255, but (uint8_t)256 is 0, so a
   uint8_t parameter would return 0 -- the opposite of saturation. */

static inline uint32_t cnx_clamp_add_u32(uint32_t a, uint64_t b) {
    if (b > (uint64_t)(UINT32_MAX - a)) return UINT32_MAX;
    return (uint32_t)(a + (uint32_t)b);
}

// test-execution
// test-adr: 057
// ADR-057: resolution local -> scope -> global, exercised where there is NO
// enclosing scope.
//
// Every other ADR-057 fixture puts its shadowing inside a `scope`, so the
// matrix rows for `top-level function` and `global variable` had no fixture at
// all -- the rule's behaviour outside a scope was untested, not merely
// unobservable. A local that shadows a file-scope name must still be renamed
// here, where the middle resolution step does not exist.
uint32_t counter = 100U;

uint32_t limit = 7U;

uint32_t doubled(uint32_t v) {
    return v + v;
}

int main(void) {
    uint32_t main__counter = 1U;
    uint32_t main__limit = 2U;
    main__counter = cnx_clamp_add_u32(main__counter, main__limit);
    uint32_t viaCall = doubled(main__counter);
    if (main__counter != 3) return 1;
    if (main__limit != 2) return 2;
    if (viaCall != 6) return 3;
    if (counter != 100) return 4;
    if (limit != 7) return 5;
    return 0;
}
