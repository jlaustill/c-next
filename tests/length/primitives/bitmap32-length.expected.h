#ifndef BITMAP32_LENGTH_TEST_H
#define BITMAP32_LENGTH_TEST_H

/**
 * Generated by C-Next Transpiler
 * Header file for cross-language interoperability
 */

#include <stdint.h>
#include <stdbool.h>

#ifdef __cplusplus
extern "C" {
#endif

/* Bitmaps */
/* Bitmap: StatusFlags
 *   bit0: bit 0
 *   bit1: bit 1
 *   bit2: bit 2
 *   bit3: bit 3
 *   bit4: bit 4
 *   bit5: bit 5
 *   bit6: bit 6
 *   bit7: bit 7
 *   bit8: bit 8
 *   bit9: bit 9
 *   bit10: bit 10
 *   bit11: bit 11
 *   bit12: bit 12
 *   bit13: bit 13
 *   bit14: bit 14
 *   bit15: bit 15
 *   bit16: bit 16
 *   bit17: bit 17
 *   bit18: bit 18
 *   bit19: bit 19
 *   bit20: bit 20
 *   bit21: bit 21
 *   bit22: bit 22
 *   bit23: bit 23
 *   bit24: bit 24
 *   bit25: bit 25
 *   bit26: bit 26
 *   bit27: bit 27
 *   bit28: bit 28
 *   bit29: bit 29
 *   bit30: bit 30
 *   bit31: bit 31
 */
typedef uint32_t StatusFlags;

/* Struct definitions */
typedef struct TestStruct {
    StatusFlags member;
} TestStruct;

/* External variables */
extern StatusFlags globalVar;
extern StatusFlags TestScope_publicMember;

/* Function prototypes */
uint32_t TestScope_getGlobalLength(void);
uint32_t TestScope_getMemberLength(void);

#ifdef __cplusplus
}
#endif

#endif /* BITMAP32_LENGTH_TEST_H */
