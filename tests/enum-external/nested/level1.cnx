// Middle level - includes level2 and uses its enum

#include "level2.cnx"

scope NestedExtEnum {
    public struct Config {
    ELevel level;
}
}

void setMedium(NestedExtEnum.Config cfg) {
    cfg.level <- ELevel.LEVEL_MEDIUM;
}
