// test-error
// Tests: sizeof operand must not have side effects per MISRA Rule 13.6 (E0602)

u32 counter <- 0;

u32 increment() {
    counter <- counter + 1;
    return counter;
}

u32 main() {
    u32 size <- sizeof(increment());
    return 0;
}
