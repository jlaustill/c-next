/**
 * Generated by C-Next Transpiler from: cross-file-provider.cnx
 * A safer C for embedded systems
 */

#include "cross-file-provider.h"

#include <stdint.h>

// ADR-044: Overflow helper functions
#include <limits.h>

/* ADR-044 / Issue #94: the second parameter is the WIDER type, not the value type.
   Narrowing it first would let an out-of-range operand truncate INTO range and defeat
   the check: cnx_clamp_add_u8(0, 256) must saturate to 255, but (uint8_t)256 is 0, so a
   uint8_t parameter would return 0 -- the opposite of saturation. */

static inline uint32_t cnx_clamp_add_u32(uint32_t a, uint64_t b) {
    if (b > (uint64_t)(UINT32_MAX - a)) return UINT32_MAX;
    return (uint32_t)(a + (uint32_t)b);
}

// Provider for the ADR-057 cross-file fixtures. Not a test itself.
//
// Declares a scope whose members are referenced BARE from a method declared in
// this same file, so the consumer's include depth -- not this file's contents --
// is what places the fixture on the relationship axis.
/* Scope: Sensor */
static uint32_t Sensor__reading = 0U;
static uint32_t Sensor__offset = 3U;

uint32_t Sensor__raw(void) {
    return 7;
}

void Sensor__refresh(void) {
    Sensor__reading = cnx_clamp_add_u32(Sensor__raw(), Sensor__offset);
}

uint32_t Sensor__value(void) {
    return Sensor__reading;
}
