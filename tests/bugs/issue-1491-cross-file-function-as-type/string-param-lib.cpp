/**
 * Generated by C-Next Transpiler from: string-param-lib.cnx
 * A safer C for embedded systems
 */

#include "string-param-lib.hpp"

#include <stdint.h>
#include <string.h>

// Helper for cross-file-string-param.test.cnx (#1491).
//
// A function-as-type with a `string<N>` parameter, reached through an include.
// This is the shape where the symbol path and the parse-tree path answered the
// same question differently, so it needs its own included declaration.
uint32_t describe(const char* label) {
    return strlen(label);
}
