/**
 * Generated by C-Next Transpiler from: transitive-type-resolves.test.cnx
 * A safer C for embedded systems
 */

#include "transitive-type-resolves.test.hpp"

// NEGATIVE CONTROL for issue #1312 — this file must stay SILENT.
// `Depth` is two include-hops away (this -> mid-layer -> deep-enum), which is
// a genuine visibility path, unlike the sibling case. A fix that rejects an
// undefined type must not reject this one; if this fixture starts producing a
// diagnostic, the fix is over-firing and the type rule has become useless.
#include <mid-layer.hpp>

#include <stdint.h>

Depth reachedThroughChain = Depth__DEPTH_A;

int main(void) {
    if (reachedThroughChain != Depth__DEPTH_A) {
        return 1;
    }
    return 0;
}
