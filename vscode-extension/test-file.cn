// Test file to demonstrate language server features
class TestClass {
    // Correct c-next syntax
    int16 correctVar <- 42;
    
    // This should show an error - using = instead of <-
    int32 errorVar = 100;
    
    // This should show a warning - using quotes instead of backticks
    String message = "This should use backticks";
    
    // This should be correct
    String correctMessage <- `This is correct syntax`;
    
    public void testMethod() {
        // This should work - Arduino function
        pinMode(LED_BUILTIN, OUTPUT);
        
        // This should show an undefined function warning
        someUndefinedFunction();
        
        // Missing semicolon (should show warning)
        int16 value <- 10
        
        digitalWrite(LED_BUILTIN, HIGH);
        delay(1000);
    }
}