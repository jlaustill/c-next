// ADR-046: fopen without c_ prefix should error
// fopen is now allowed with c_ prefix (see c-prefix-valid-fopen.test.cnx)
#include <stdio.h>

void openFile() {
    FILE file <- fopen("test.txt", "r"); // ERROR: missing c_ prefix
}
