#include `Arduino.h`;
import `Blink.cn`;

Blink blinker;

void setup() {
    Serial.begin(115200);
    blinker.setup(1000);
}

void loop() {
    Serial.println(`Loopy Loop! ${micros()}`);
    
    blinker.loop();
}