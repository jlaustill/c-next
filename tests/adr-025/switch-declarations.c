/**
 * Generated by C-Next Transpiler from: switch-declarations.cnx
 * A safer C for embedded systems
 */

#include "switch-declarations.h"

#include <stdint.h>

// Helper (not a test): the enum the cross-file switch fixtures switch on.
// Exhaustiveness is counted against the enum's MEMBERS, so when the enum is
// declared elsewhere the count has to reach across the include boundary.
const uint32_t SWITCH_MARKER = 1U;
