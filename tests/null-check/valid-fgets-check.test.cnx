// ADR-046: Valid inline NULL check pattern for stream functions
#include <stdio.h>

string<64> buffer;

void readInput() {
    if (fgets(buffer, buffer.size, stdin) != NULL) {
        printf("Got: %s", buffer);
    }
}
