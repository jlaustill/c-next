// Helper file: enum and struct definitions for cross-file struct member test

enum MyEnum {
    A <- 0,
    B <- 1,
    NONE <- 0xFF
}

struct TItem {
    MyEnum assigned;
    u8 padding;
}

struct Config {
    TItem[4] items;
}

const u8 COUNT <- 4;

Config config;
