// Helper (not a test): puts string-declarations.cnx two hops away, covering
// the `imported transitive` column. It declares something of its own so a
// header is written for it (#1234).

#include "string-declarations.cnx"

const u32 STRING_RELAY_MARKER <- 2;
