/**
 * Generated by C-Next Transpiler from: scoped-enum-array-dimension-header.test.cnx
 * A safer C for embedded systems
 */

#include "scoped-enum-array-dimension-header.test.hpp"

#include <stdint.h>

// test-execution
// Header and implementation must derive an array dimension the same way.
//
// The dimension arrives at the header adapter as C-Next SOURCE (`State.COUNT`),
// not as a generated C name, so it has to be resolved with the same scope
// awareness the .c path uses. Sharing the join function is not enough — both
// sides must also agree on WHAT to join.
//
// Before the fix the header emitted `State__COUNT` while the .c emitted
// `Motor__State__COUNT`, and the header did not compile. The `this.` spelling
// was worse: `this__State__COUNT`, with `this` joined in as a name component.
/* Scope: Motor */
uint8_t Motor__fromScope[Motor__State__COUNT] = {};
uint8_t Motor__fromGlobal[Global__COUNT] = {};
uint8_t Motor__viaThis[Motor__State__COUNT] = {};

int main(void) {
    Motor__fromScope[0] = 11;
    Motor__fromGlobal[0] = 22;
    Motor__viaThis[0] = 33;
    if (Motor__fromScope[0U] != 11) return 1;
    if (Motor__fromGlobal[0U] != 22) return 2;
    if (Motor__viaThis[0U] != 33) return 3;
    Motor__fromScope[1] = 44;
    if (Motor__fromScope[1U] != 44) return 4;
    return 0;
}
