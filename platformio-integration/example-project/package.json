{
  "name": "teensy-cnext-project",
  "version": "1.0.0",
  "description": "Example Teensy project using c-next with PlatformIO",
  "scripts": {
    "build": "pio run",
    "upload": "pio run --target upload",
    "monitor": "pio device monitor"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "glob": "^10.3.0"
  },
  "dependencies": {
    "glob": "^10.3.0"
  }
}