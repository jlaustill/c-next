// ADR-030: Self-recursion is forbidden per MISRA C:2012 Rule 17.2
// A function cannot call itself, directly or indirectly

void countdown(u32 n) {
    if (n = 0) { return; }
    countdown(n - 1);
}
