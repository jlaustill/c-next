/**
 * Generated by C-Next Transpiler from: leaf.cnx
 * A safer C for embedded systems
 */

#include "leaf.h"

#include <stdint.h>

// Two-hop helper for entry.test.cnx (#1508). Nothing in the entry includes this
// file directly -- it is reached only through `mid.cnx`, which is the whole
// point: its object has to be linked even though the entry never names it.
uint32_t leafValue(void) {
    return 5U;
}
