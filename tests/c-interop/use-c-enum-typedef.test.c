/**
 * Generated by C-Next Transpiler from: use-c-enum-typedef.test.cnx
 * A safer C for embedded systems
 */

// test-c-only
// test-execution
// test-coverage: 30-use-c-enum-typedef
// Tests: Using typedef'd C enums
// ADR-010: Enum typedef import from C headers
#include "c-interop/enums.h"

#include <stdint.h>

int main(void) {
    Priority low = PRIORITY_LOW;
    Priority med = PRIORITY_MEDIUM;
    Priority high = PRIORITY_HIGH;
    Priority crit = PRIORITY_CRITICAL;
    if (low != 0) return 1U;
    if (med != 50) return 2U;
    if (high != 100) return 3U;
    if (crit != 255) return 4U;
    Priority p = PRIORITY_HIGH;
    if (p != PRIORITY_HIGH) return 5U;
    if (p < PRIORITY_MEDIUM) return 6U;
    Priority threshold = PRIORITY_MEDIUM;
    if (high > threshold) {
    } else {
        return 7U;
    }
    Permissions perm = FLAG_READ;
    perm = perm | FLAG_WRITE;
    if (perm != 3) return 8U;
    return 0U;
}
