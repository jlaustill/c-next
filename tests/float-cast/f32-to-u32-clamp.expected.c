/**
 * Generated by C-Next Transpiler
 * A safer C for embedded systems
 */

#include <stdint.h>
#include <limits.h>

// test-execution
// Tests: f32 to u32 cast should clamp values to 0-4294967295 range
int main(void) {
    float over = 5000000000.0;
    float negative = -100.0;
    float in_range = 1000000.0;
    uint32_t result = ((over) > ((float)UINT32_MAX) ? UINT32_MAX : (over) < 0.0f ? 0 : (uint32_t)(over));
    if (result != 4294967295) return 1;
    result = ((negative) > ((float)UINT32_MAX) ? UINT32_MAX : (negative) < 0.0f ? 0 : (uint32_t)(negative));
    if (result != 0) return 2;
    result = ((in_range) > ((float)UINT32_MAX) ? UINT32_MAX : (in_range) < 0.0f ? 0 : (uint32_t)(in_range));
    if (result != 1000000) return 3;
    return 0;
}
