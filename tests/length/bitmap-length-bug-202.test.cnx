// test-execution
// Regression test for Issue #202: Bitmap .length property
// Verifies bitmap8 variable .length returns 8 (was returning 0)
bitmap8 Flags {
    bit0,
    bit1,
    bit2,
    bit3,
    bit4,
    bit5,
    bit6,
    bit7
}

i32 main() {
    Flags myVar;

    // bitmap8 .length should return 8
    if (myVar.length != 8) {
        return 1;
    }

    return 0;
}
