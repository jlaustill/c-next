// Test that bare global identifiers inside scopes produce errors

const u8 globalValue <- 10;

scope Motor {
    u8 badFunction() {
        // This should produce an error: bare identifier referencing global
        return globalValue;  // ERROR: should use global.globalValue
    }
}
