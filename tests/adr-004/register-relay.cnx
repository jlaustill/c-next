// Helper (not a test): puts register-declarations.cnx two hops away, covering
// the `imported transitive` column. It declares something of its own so a
// header is written for it (#1234).

#include "register-declarations.cnx"

const u32 REGISTER_RELAY_MARKER <- 2;
