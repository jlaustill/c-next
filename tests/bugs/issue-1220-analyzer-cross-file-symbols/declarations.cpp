/**
 * Generated by C-Next Transpiler from: declarations.cnx
 * A safer C for embedded systems
 */

#include "declarations.hpp"

#include <stdint.h>
#include <stdbool.h>

// Helper (not a test): declarations the cross-file fixtures resolve against.
// Issue #1220 - analyzers built local parse-tree caches and never consulted the
// symbol table, so every one of these was invisible across an include boundary.
extern const uint32_t ZERO = 0U;

extern const uint32_t NONZERO = 4U;

float floatValue = 5.0;

int32_t signedValue = -8;

int32_t otherSignedValue = -2;

uint32_t importedUnsignedValue = 8U;

bool flagValue = true;
