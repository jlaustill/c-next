/**
 * Generated by C-Next Transpiler
 * A safer C for embedded systems
 */

#include <stdint.h>

// ADR-044: Overflow helper functions
#include <limits.h>

static inline int8_t cnx_clamp_add_i8(int8_t a, int8_t b) {
    if (b > 0 && a > INT8_MAX - b) return INT8_MAX;
    if (b < 0 && a < INT8_MIN - b) return INT8_MIN;
    return a + b;
}

static inline int32_t cnx_clamp_mul_i32(int32_t a, int32_t b) {
    if (a == 0 || b == 0) return 0;
    if (a > 0 && b > 0 && a > INT32_MAX / b) return INT32_MAX;
    if (a < 0 && b < 0 && a < INT32_MAX / b) return INT32_MAX;
    if (a > 0 && b < 0 && b < INT32_MIN / a) return INT32_MIN;
    if (a < 0 && b > 0 && a < INT32_MIN / b) return INT32_MIN;
    return a * b;
}

static inline int8_t cnx_clamp_sub_i8(int8_t a, int8_t b) {
    if (b < 0 && a > INT8_MAX + b) return INT8_MAX;
    if (b > 0 && a < INT8_MIN + b) return INT8_MIN;
    return a - b;
}

// Test: Signed integer overflow handling
// Signed types need to check both overflow and underflow
int8_t small_signed = 100;

int32_t large_signed = 2000000000;

void test_signed_ops(void) {
    small_signed = cnx_clamp_add_i8(small_signed, 50);
    small_signed = cnx_clamp_sub_i8(small_signed, 200);
    large_signed = cnx_clamp_mul_i32(large_signed, 2);
}
