/**
 * Generated by C-Next Transpiler from: shadowing-test2.test.cnx
 * A safer C for embedded systems
 */

#include "shadowing-test2.test.h"

#include <stdint.h>

// test-execution
// Test: Scope shadowing between global and scope (no const) (ADR-016)
// Scope member should shadow global with same name
uint8_t globalFlags = 0U;

// Global with different name
uint8_t flags = 0U;

// Global 'flags'
/* Scope: Control */
static uint8_t Control_flags = 0b11111111U;

void Control_setFlag(void) {
    Control_flags = 0b10101010U;
}

uint8_t Control_getFlags(void) {
    return Control_flags;
}

int main(void) {
    uint8_t val = Control_getFlags();
    if (val != 0b11111111) return 1;
    Control_setFlag();
    val = Control_getFlags();
    if (val != 0b10101010) return 2;
    if (flags != 0) return 3;
    if (globalFlags != 0) return 4;
    return 0;
}
