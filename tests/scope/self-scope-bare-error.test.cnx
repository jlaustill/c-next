// Test: Referencing own scope by name should error
// ADR-016: Must use this. prefix for self-referential access

scope Counter {
    i32 value <- 100;

    void test() {
        // ERROR: Must use this.value instead of Counter.value
        Counter.value <- 50;
    }
}

void main() {
}
