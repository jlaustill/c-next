/**
 * Generated by C-Next Transpiler from: shadow-local-every-position.test.cnx
 * A safer C for embedded systems
 */

#include "shadow-local-every-position.test.h"

#include <stdint.h>
#include <string.h>

// ADR-044: Overflow helper functions
#include <limits.h>

/* ADR-044 / Issue #94: the second parameter is the WIDER type, not the value type.
   Narrowing it first would let an out-of-range operand truncate INTO range and defeat
   the check: cnx_clamp_add_u8(0, 256) must saturate to 255, but (uint8_t)256 is 0, so a
   uint8_t parameter would return 0 -- the opposite of saturation. */

static inline uint32_t cnx_clamp_add_u32(uint32_t a, uint64_t b) {
    if (b > (uint64_t)(UINT32_MAX - a)) return UINT32_MAX;
    return (uint32_t)(a + (uint32_t)b);
}

static inline uint32_t cnx_clamp_sub_u32(uint32_t a, uint64_t b) {
    if (b > (uint64_t)a) return 0;
    return (uint32_t)(a - (uint32_t)b);
}

// test-adr: 057
// ADR-057: coverage sweep, derived from the EMISSION SITES rather than from the
// defect history.
//
// A local's identifier reaches the generated C through several independent
// emitters -- declarations, expression references, assignment targets, and the
// resolvers for `sizeof` and the length properties. Each applies the shadow
// rename separately, so each is a place the rename can be missed. Three
// successive reviews of #1289 each found one more (declarations, then writes,
// then `sizeof`), because coverage was written from bugs already found.
//
// This fixture uses a shadowing local in every syntactic position the grammar
// admits and writes `global.` NOWHERE. The bare names below must therefore
// never appear inside the generated function body -- only in the file-scope
// declarations they shadow. A missed emission site shows up as a snapshot diff
// containing a bare name, and, where the shapes differ, as a compile failure.
uint32_t probe = 1000U;

uint32_t parr[4] = {1U, 2U, 3U, 4U};

uint8_t pbits = 0U;

uint32_t pidx = 3U;

uint32_t pdown = 50U;

Holder pcfg = { .x = 9U };

char pstr[17] = "global";

uint32_t takes(uint32_t v) {
    return v;
}

/* Scope: S */

uint32_t S__positions(uint32_t flag) {
    uint32_t S__positions__probe = 1U;
    uint32_t S__positions__parr[4] = {5U, 6U, 7U, 8U};
    uint8_t S__positions__pbits = 0U;
    uint32_t S__positions__pidx = 1U;
    uint32_t S__positions__pdown = 2U;
    Holder S__positions__pcfg = { .x = 1U };
    char S__positions__pstr[9] = "ab";
    uint32_t acc = 0U;
    S__positions__probe = 2U;
    S__positions__probe = cnx_clamp_add_u32(S__positions__probe, 1U);
    acc = cnx_clamp_add_u32(acc, S__positions__probe);
    acc = cnx_clamp_add_u32(acc, cnx_clamp_add_u32(S__positions__probe, 1U));
    acc = cnx_clamp_add_u32(acc, takes(S__positions__probe));
    acc = cnx_clamp_add_u32(acc, (flag == 1) ? S__positions__probe : 0U);
    if (S__positions__probe > 0) {
        acc = cnx_clamp_add_u32(acc, 1U);
    }
    while (S__positions__pdown > 0) {
        S__positions__pdown = cnx_clamp_sub_u32(S__positions__pdown, 1U);
    }
    acc = cnx_clamp_add_u32(acc, ((S__positions__probe) & ((1U << 4U) - 1)));
    S__positions__parr[0] = 5U;
    acc = cnx_clamp_add_u32(acc, S__positions__parr[1U]);
    acc = cnx_clamp_add_u32(acc, S__positions__parr[S__positions__pidx]);
    acc = cnx_clamp_add_u32(acc, 4);
    acc = cnx_clamp_add_u32(acc, sizeof(S__positions__parr));
    S__positions__pbits = (uint8_t)((S__positions__pbits & ~(((1U << 4) - 1) << 0)) | ((3 & ((1U << 4) - 1)) << 0));
    acc = cnx_clamp_add_u32(acc, S__positions__pbits);
    S__positions__pcfg.x = 7U;
    acc = cnx_clamp_add_u32(acc, S__positions__pcfg.x);
    acc = cnx_clamp_add_u32(acc, sizeof(S__positions__pcfg.x));
    (void) strncpy(S__positions__pstr, "cd", 8); S__positions__pstr[8] = '\0';
    acc = cnx_clamp_add_u32(acc, strlen(S__positions__pstr));
    Holder made = { .x = S__positions__probe };
    acc = cnx_clamp_add_u32(acc, made.x);
    uint32_t init[2] = {S__positions__probe, 2U};
    acc = cnx_clamp_add_u32(acc, init[0U]);
    return acc;
}
