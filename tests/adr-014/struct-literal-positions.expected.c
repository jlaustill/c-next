/**
 * Generated by C-Next Transpiler from: struct-literal-positions.test.cnx
 * A safer C for embedded systems
 */

#include "struct-literal-positions.test.h"

#include <stdint.h>

// ADR-044: Overflow helper functions
#include <limits.h>

/* ADR-044 / Issue #94: the second parameter is the WIDER type, not the value type.
   Narrowing it first would let an out-of-range operand truncate INTO range and defeat
   the check: cnx_clamp_add_u8(0, 256) must saturate to 255, but (uint8_t)256 is 0, so a
   uint8_t parameter would return 0 -- the opposite of saturation. */

static inline uint32_t cnx_clamp_add_u32(uint32_t a, uint64_t b) {
    if (b > (uint64_t)(UINT32_MAX - a)) return UINT32_MAX;
    return (uint32_t)(a + (uint32_t)b);
}

// test-execution
// test-adr: 014
// #1277: a struct literal takes its type from the position it stands in. Every
// position that can supply one, exercised and its value checked -- the three
// at the end were rejected as "Cannot infer struct type" until #1322, while the
// identical literal assigned to a local first compiled.
Point fromReturn(void) {
    return (Point){ .x = 10U, .y = 20U };
}

/* Scope: Shapes */

Point Shapes__fromScopeMethod(void) {
    return (Point){ .x = 30U, .y = 40U };
}

Point atFileScope = { .x = 1U, .y = 2U };

int main(void) {
    Point declared = { .x = 3U, .y = 4U };
    if (declared.x != 3) return 1U;
    if (declared.y != 4) return 2U;
    declared = (Point){ .x = 5U, .y = 6U };
    if (declared.x != 5) return 3U;
    Box box = { .corner = { .x = 7U, .y = 8U } };
    if (box.corner.y != 8) return 4U;
    if (atFileScope.y != 2) return 6U;
    Point r = fromReturn();
    if (r.x != 10) return 7U;
    if (r.y != 20) return 8U;
    Point s = Shapes__fromScopeMethod();
    if (s.x != 30) return 9U;
    if (s.y != 40) return 10U;
    uint32_t total = 0U;
    for (Point p = (Point){ .x = 2U, .y = 3U }; p.x < 4; p.x += 1) {
        total = cnx_clamp_add_u32(total, p.y);
    }
    if (total != 6) return 11U;
    return 0U;
}
