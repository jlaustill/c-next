/**
 * Generated by C-Next Transpiler from: string-array-element-count.test.cnx
 * A safer C for embedded systems
 */

#include <stdint.h>
#include <string.h>

// test-execution
// Issue #1029: .element_count should work on string arrays inside functions
int main(void) {
    char items[4][33] = {0};
    (void)strncpy(items[0], "One", 32);
    (void)strncpy(items[1], "Two", 32);
    (void)strncpy(items[2], "Three", 32);
    (void)strncpy(items[3], "Four", 32);
    if (4 != 4) {
        return 1;
    }
    uint32_t count = 0U;
    uint32_t i = 0U;
    while (i < 4) {
        count = count + 1U;
        i = i + 1U;
    }
    if (count != 4) {
        return 2;
    }
    return 0;
}
