/**
 * Generated by C-Next Transpiler from: while-infinite.test.cnx
 * A safer C for embedded systems
 */

#include <stdint.h>
#include <stdbool.h>

// ADR-044: Overflow helper functions
#include <limits.h>

static inline uint32_t cnx_clamp_add_u32(uint32_t a, uint64_t b) {
    if (b > (uint64_t)(UINT32_MAX - a)) return UINT32_MAX;
    uint32_t result;
    if (__builtin_add_overflow(a, (uint32_t)b, &result)) return UINT32_MAX;
    return result;
}

// Coverage: Section 7.2 - Infinite while loop (while true)
// Tests: while(true) syntax compiles correctly
// Note: Not an execution test since infinite loop would never terminate
uint32_t counter = 0U;

void runForever(void) {
    while (true) {
        counter = cnx_clamp_add_u32(counter, 1U);
        if (counter > 100) {
            break;
        }
    }
}

void anotherInfinitePattern(void) {
    bool running = true;
    while (running == true) {
        counter = cnx_clamp_add_u32(counter, 1U);
        if (counter > 200) {
            running = false;
        }
    }
}

int main(void) {
    runForever();
    anotherInfinitePattern();
}
