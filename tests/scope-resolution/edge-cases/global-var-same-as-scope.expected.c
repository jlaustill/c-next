/**
 * Generated by C-Next Transpiler from: global-var-same-as-scope.test.cnx
 * A safer C for embedded systems
 */

#include "global-var-same-as-scope.test.h"

#include <stdint.h>

// ADR-044: Overflow helper functions
#include <limits.h>

/* ADR-044 / Issue #94: the second parameter is the WIDER type, not the value type.
   Narrowing it first would let an out-of-range operand truncate INTO range and defeat
   the check: cnx_clamp_add_u8(0, 256) must saturate to 255, but (uint8_t)256 is 0, so a
   uint8_t parameter would return 0 -- the opposite of saturation. */

static inline uint32_t cnx_clamp_add_u32(uint32_t a, uint64_t b) {
    if (b > (uint64_t)(UINT32_MAX - a)) return UINT32_MAX;
    return (uint32_t)(a + (uint32_t)b);
}

// Test global variable with same name as scope
// Both can coexist - scope becomes prefix, variable stays as-is
// test-execution
uint8_t LED = 5U;

// Global variable named same as scope
/* Scope: LED */
uint8_t LED__brightness = 100U;

void LED__on(void) {
    LED__brightness = 255U;
}

/* Scope: Motor */

uint32_t Motor__test(void) {
    uint8_t val = LED;
    LED__on();
    uint8_t bright = LED__brightness;
    uint32_t result = val;
    result = cnx_clamp_add_u32(result, bright);
    return result;
}

int main(void) {
    uint32_t result = Motor__test();
    if (result != 260) return 1;
    return 0;
}
