// Test: Bare enum access from inside a scope should error
// ADR-016: Must use global. prefix for cross-scope enum access

enum GlobalState { IDLE, RUNNING, STOPPED }

scope Motor {
    void test() {
        // ERROR: Must use global.GlobalState.IDLE instead of GlobalState.IDLE
        GlobalState current <- GlobalState.IDLE;
    }
}

void main() {
}
