/**
 * Generated by C-Next Transpiler from: const-bool-variable.test.cnx
 * A safer C for embedded systems
 */

#include <stdint.h>
#include <stdbool.h>

// test-execution
// ADR-013: Const bool variable
// Tests: const bool declaration and read access
const bool FEATURE_ENABLED = true;

const bool DEBUG_MODE = false;

int main(void) {
    bool enabled = FEATURE_ENABLED;
    if (!enabled) return 1;
    bool debug = DEBUG_MODE;
    if (debug) return 2;
    if (!FEATURE_ENABLED) return 3;
    if (DEBUG_MODE) return 4;
    bool result = FEATURE_ENABLED && !DEBUG_MODE;
    if (!result) return 5;
    uint32_t value = (FEATURE_ENABLED == true) ? 100U : 0U;
    if (value != 100) return 6;
    uint32_t debugVal = (DEBUG_MODE == true) ? 1U : 0U;
    if (debugVal != 0) return 7;
    return 0;
}
