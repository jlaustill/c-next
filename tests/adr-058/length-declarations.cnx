// Helper (not a test): the declarations the cross-file ADR-058 fixtures ask
// length properties of. The rule needs the subject's TYPE, which lives here.
u32 sharedScalar <- 5;
u32[4] sharedArray;

const u32 LENGTH_MARKER <- 1;
