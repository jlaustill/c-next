/**
 * Generated by C-Next Transpiler from: length-property-enum-and-bitmap.test.cnx
 * A safer C for embedded systems
 */

#include "length-property-enum-and-bitmap.test.h"

#include <stdint.h>

// test-execution
// test-adr: 058
// An enum and a bitmap both have a fixed width, so .bit_length and .byte_length
// answer on them. The first version of the 2.1 rule sized only primitives and
// rejected both; four fixtures caught it. This one is here so that cannot
// silently return: it EXECUTES, and asserts the widths.
int main(void) {
    Color c = Color__RED;
    if (32 != 32) return 1;
    if (4 != 4) return 2;
    Flags f = {0};
    f = (f & ~(0xFU << 0)) | ((1 & 0xFU) << 0);
    if (16 != 16) return 3;
    if (2 != 2) return 4;
    return 0;
}
