// Helper (not a test): puts slice-declarations.cnx two hops away, covering the
// `imported transitive` column. It declares something of its own so a header is
// written for it (#1234).

#include "slice-declarations.cnx"

const u32 SLICE_RELAY_MARKER <- 2;
