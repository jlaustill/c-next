// Helper (not a test): puts length-declarations.cnx two hops away, covering
// the `imported transitive` column. It declares something of its own so a
// header is written for it (#1234).

#include "length-declarations.cnx"

const u32 LENGTH_RELAY_MARKER <- 2;
