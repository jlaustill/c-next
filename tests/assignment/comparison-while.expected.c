/**
 * Generated by C-Next Transpiler from: comparison-while.test.cnx
 * A safer C for embedded systems
 */

#include "comparison-while.test.h"

#include <stdint.h>

// ADR-044: Overflow helper functions
#include <limits.h>

/* ADR-044 / Issue #94: the second parameter is the WIDER type, not the value type.
   Narrowing it first would let an out-of-range operand truncate INTO range and defeat
   the check: cnx_clamp_add_u8(0, 256) must saturate to 255, but (uint8_t)256 is 0, so a
   uint8_t parameter would return 0 -- the opposite of saturation. */

static inline uint32_t cnx_clamp_add_u32(uint32_t a, uint64_t b) {
    if (b > (uint64_t)(UINT32_MAX - a)) return UINT32_MAX;
    return (uint32_t)(a + (uint32_t)b);
}

// test-execution
// ADR-001: Comparison in while loops
// Tests: = operator transpiles to == in while conditions
uint32_t sumUntilLimit(uint32_t limit) {
    uint32_t sum = 0U;
    uint32_t i = 0U;
    while (i != limit) {
        sum = cnx_clamp_add_u32(sum, i);
        i = cnx_clamp_add_u32(i, 1U);
    }
    return sum;
}

int main(void) {
    uint32_t five = 5U;
    uint32_t zero = 0U;
    uint32_t three = 3U;
    uint32_t result1 = sumUntilLimit(five);
    if (result1 != 10) {
        return 1;
    }
    uint32_t result2 = sumUntilLimit(zero);
    if (result2 != 0) {
        return 1;
    }
    uint32_t result3 = sumUntilLimit(three);
    if (result3 != 3) {
        return 1;
    }
    return 0;
}
