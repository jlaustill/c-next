/**
 * Generated by C-Next Transpiler from: nested-block-indentation.test.cnx
 * A safer C for embedded systems
 */

#include <stdint.h>
#include <stdbool.h>
#include <string.h>

// test-execution
// Issue #1037: string init / concatenation / substring DECLARATIONS inside a
// nested block (depth 2) must keep their continuation lines aligned with the
// declaration line. This is the recursive-indent case the multi-line emitter
// is most fragile at — generateBlock prefixes every line per nesting level.
int main(void) {
    bool gate = true;
    if (gate == true) {
        char base[17] = "Hello";
        char copied[33] = "";
        strncpy(copied, base, 32); copied[32] = '\0';
        char joined[33] = "";
        strncpy(joined, base, 32);
        strncat(joined, base, 32 - strlen(joined));
        joined[32] = '\0';
        char part[9] = "";
        strncpy(part, base + 0, 3);
        part[3] = '\0';
        if (strcmp(copied, "Hello") != 0) return 1;
        if (strcmp(joined, "HelloHello") != 0) return 2;
        if (strcmp(part, "Hel") != 0) return 3;
    }
    return 0;
}
