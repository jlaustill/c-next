/* test-coverage: 5.5-shift-beyond-width-error */ // Test i32 shift beyond width - should be ERROR
// Coverage: Section 5.5-5.6 error case - shift beyond type width for signed types
void testI32ShiftBeyondWidth() {
    i32 a <- 1;

    // i32 is 32 bits, so shifting by 32 or more is undefined/error
    i32 bad_shift <- a >> 32; // ERROR: shift amount >= type width
}
