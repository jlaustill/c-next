/**
 * Generated by C-Next Transpiler from: SerialHandler.test.cnx
 * A safer C for embedded systems
 */

#include "SerialHandler.test.hpp"

// test-cpp-only
// Issue #580: Incorrect const inference when parameter is only passed to mutating function
// handleReset was incorrectly marked const even though it passes config to reset() which mutates it
#include "Config.hpp"
#include "CommandHandler.hpp"
#include "Serial.hpp"

#include <stdint.h>

/* Scope: SerialHandler */

void SerialHandler__handleReset(Config& config) {
    Serial__println(1);
    CommandHandler__reset(config);
    Serial__println(2);
}

void SerialHandler__handlePreset(Config& config) {
    if (config.value < 10) {
        Serial__println(3);
        return;
    }
    uint8_t input = 1U;
    uint8_t preset = 2U;
    CommandHandler__applyPreset(config, input, preset);
}

void SerialHandler__handleSave(Config& config) {
    CommandHandler__reset(config);
}

int main(void) {
    Config c = { .value = 0U };
    SerialHandler__handleReset(c);
}
