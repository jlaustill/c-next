// Test that bare identifiers inside scopes produce errors

const u8 globalValue <- 10;

scope Motor {
    const u8 localValue <- 5;

    u8 badFunction() {
        // This should produce an error: bare identifier referencing scope member
        return localValue;  // ERROR: should use this.localValue
    }
}
