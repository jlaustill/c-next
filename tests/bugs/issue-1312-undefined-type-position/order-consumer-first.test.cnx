// test-error
// #1399 review: the other order. This one always reported the error, so on its
// own it could not detect the leak — it is the half that made the pair
// disagree, and only the pair is the assertion.
#include <order-consumer.cnx>
#include <order-lib.cnx>

u32 main() {
    return 0;
}
