/**
 * Generated by C-Next Transpiler
 * A safer C for embedded systems
 */

#include <stdint.h>
#include <limits.h>

// test-execution
// Tests: f64 to i64 cast should clamp values within range
// Note: f64 can represent values beyond i64 range but with precision loss
int main(void) {
    double in_range = 1000000000000.0;
    double negative = -500000000000.0;
    int64_t result = ((in_range) > ((double)INT64_MAX) ? INT64_MAX : (in_range) < ((double)INT64_MIN) ? INT64_MIN : static_cast<int64_t>((in_range)));
    if (result != 1000000000000) return 1;
    result = ((negative) > ((double)INT64_MAX) ? INT64_MAX : (negative) < ((double)INT64_MIN) ? INT64_MIN : static_cast<int64_t>((negative)));
    if (result != -500000000000) return 2;
    return 0;
}
