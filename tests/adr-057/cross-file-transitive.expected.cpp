/**
 * Generated by C-Next Transpiler from: cross-file-transitive.test.cnx
 * A safer C for embedded systems
 */

#include "cross-file-transitive.test.hpp"

// test-execution
// test-adr: 057, 016
// ADR-057 resolution two hops from the provider (imported transitive).
//
// The companion to cross-file-direct: same rule, one include deeper. The
// relationship axis takes the DEEPEST chain reachable from the fixture, so this
// file must reach `cross-file-provider.cnx` through `cross-file-middle.cnx`
// rather than including it directly -- an include added here would collapse the
// chain and silently move the fixture back into the `direct` column.
#include "cross-file-middle.hpp"

#include <stdint.h>

// ADR-044: Overflow helper functions
#include <limits.h>

/* ADR-044 / Issue #94: the second parameter is the WIDER type, not the value type.
   Narrowing it first would let an out-of-range operand truncate INTO range and defeat
   the check: cnx_clamp_add_u8(0, 256) must saturate to 255, but (uint8_t)256 is 0, so a
   uint8_t parameter would return 0 -- the opposite of saturation. */

static inline uint32_t cnx_clamp_add_u32(uint32_t a, uint64_t b) {
    if (b > (uint64_t)(UINT32_MAX - a)) return UINT32_MAX;
    return (uint32_t)(a + (uint32_t)b);
}

/* Scope: Report */
static uint32_t Report__tally = 0U;
static uint32_t Report__weight = 5U;
static uint32_t Report__primed = 3;

uint32_t Report__factor(void) {
    return Report__weight;
}

void Report__add(uint32_t sample) {
    Report__tally = cnx_clamp_add_u32(cnx_clamp_add_u32(Report__tally, Report__factor()), sample);
}

uint32_t Report__readTally(void) {
    return Report__tally;
}

// A top-level function with a shadowing local: ADR-057 with no enclosing scope,
// in a fixture that reaches another file. `shadow-outside-any-scope` covers this
// same-file; the include boundary is a separate matrix column.
uint32_t spare = 40U;

uint32_t detached(void) {
    uint32_t detached__spare = 6U;
    return detached__spare;
}

int main(void) {
    uint32_t smoothed = Filter__smooth(1U);
    if (smoothed != 3) return 1;
    Report__add(2U);
    uint32_t tally = Report__readTally();
    if (tally != 7) return 2;
    uint32_t detachedValue = detached();
    if (detachedValue != 6) return 3;
    if (spare != 40) return 4;
    return 0;
}
