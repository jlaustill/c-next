/**
 * Generated by C-Next Transpiler from: const-array-param.test.cnx
 * A safer C for embedded systems
 */

#include "const-array-param.test.hpp"

// test-execution
// Issue #986: Array params transpile as const when passed to C APIs
// ADR-006 says all params are pass-by-reference as mutable, but array params
// that are not directly modified get auto-const, breaking C API compatibility.
#include "fake_lib.h"

#include <stdint.h>

/* Scope: Handler */

void Handler_process(uint8_t data[8]) {
    process_data(data);
}

int main(void) {
    uint8_t buffer[8] = {0};
    buffer[0] = 0U;
    buffer[1] = 0U;
    Handler_process(buffer);
    if (buffer[0U] != 0xAA) return 1;
    if (buffer[1U] != 0xBB) return 2;
    return 0;
}
