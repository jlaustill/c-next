/**
 * Generated by C-Next Transpiler from: do-while-init.test.cnx
 * A safer C for embedded systems
 */

#include "do-while-init.test.hpp"

#include <stdint.h>
#include <string.h>

// ADR-044: Overflow helper functions
#include <limits.h>

/* ADR-044 / Issue #94: the second parameter is the WIDER type, not the value type.
   Narrowing it first would let an out-of-range operand truncate INTO range and defeat
   the check: cnx_clamp_add_u8(0, 256) must saturate to 255, but (uint8_t)256 is 0, so a
   uint8_t parameter would return 0 -- the opposite of saturation. */

static inline uint8_t cnx_clamp_add_u8(uint8_t a, uint32_t b) {
    if (b > (uint32_t)(UINT8_MAX - a)) return UINT8_MAX;
    return (uint8_t)(a + (uint8_t)b);
}

// test-execution
// Issue #1019 review feedback: do-while assignment should be detected
// A scope member assigned inside a do-while body should be readable elsewhere
/* Scope: S */
static char S__buf[17] = "";

void S__init(void) {
    uint8_t i = 0U;
    do {
        (void) strncpy(S__buf, "initialized", 16); S__buf[16] = '\0';
        i = cnx_clamp_add_u8(i, 1U);
    } while (i < 1);
}

uint8_t S__len(void) {
    return strlen(S__buf);
}

int main(void) {
    S__init();
    uint8_t length = S__len();
    if (length != 11) return 1;
    return 0;
}
